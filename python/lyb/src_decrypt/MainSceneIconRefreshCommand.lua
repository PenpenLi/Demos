
-- require "main.view.challenge.ChallengeMediator";
-- require "main.view.littleHelper.LittleHelperMediator";

MainSceneIconRefreshCommand=class(Command);

function MainSceneIconRefreshCommand:ctor()
	self.class=MainSceneIconRefreshCommand;
end

function MainSceneIconRefreshCommand:execute()
  -- if MainSceneMediator then
  -- 	local mainSceneMediator=self:retrieveMediator(MainSceneMediator.name);
  -- 	if mainSceneMediator then
  -- 		mainSceneMediator:refreshIcon();
  -- 	end
  -- end
end