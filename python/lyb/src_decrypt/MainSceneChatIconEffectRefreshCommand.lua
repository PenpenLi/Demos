
-- require "main.view.challenge.ChallengeMediator";
-- require "main.view.littleHelper.LittleHelperMediator";

MainSceneChatIconEffectRefreshCommand=class(Command);

function MainSceneChatIconEffectRefreshCommand:ctor()
	self.class=MainSceneChatIconEffectRefreshCommand;
end

function MainSceneChatIconEffectRefreshCommand:execute()
  -- if MainSceneMediator then
  -- 	local mainSceneMediator=self:retrieveMediator(MainSceneMediator.name);
  -- 	if mainSceneMediator then
  -- 		mainSceneMediator:refreshChatIconEffect();
  -- 	end
  -- end
  -- if SmallChatMediator then
  --   local smallChatMediator=self:retrieveMediator(SmallChatMediator.name);
  --   if smallChatMediator then
  --     smallChatMediator:refreshEffect();
  --   end
  -- end
end